# Configuration file for `prek`, a git hook framework written in Rust.
# See https://prek.j178.dev for more information.
#:schema https://www.schemastore.org/prek.json

[[repos]]
repo = "builtin"
hooks = [
  { id = "check-added-large-files", name = "Check added large files", args = [
    "--maxkb=1000",
  ] },
  { id = "check-toml", name = "Check TOML" },
  { id = "check-yaml", name = "Check YAML" },
  { id = "check-json", name = "Check JSON" },
  { id = "end-of-file-fixer", name = "End of file fixer" },
  { id = "trailing-whitespace", name = "Trailing whitespace" },
  { id = "check-merge-conflict", name = "Check Merge Conflict", args = [
    "--assume-in-merge",
  ] },
  { id = "detect-private-key", name = "Detect Private Key" },
]
[[repos]]
repo = "https://github.com/crate-ci/typos"
rev = "v1.47.2"
hooks = [{ id = "typos", name = "Typos Linter" }]
[[repos]]
repo = "https://github.com/python-jsonschema/check-jsonschema"
rev = "0.35.0"
hooks = [
  { id = "check-github-actions", name = "Validate GitHub Actions", args = [
    "--verbose",
  ] },
]
[[repos]]
repo = "https://github.com/rhysd/actionlint"
rev = "v1.7.12"
hooks = [{ id = "actionlint", name = "Actionlint Linter" }]
[[repos]]
repo = "https://github.com/adrienverge/yamllint.git"
rev = "v1.37.1"
hooks = [{ id = "yamllint", name = "YAML Linter", args = ["-c", ".github/yamllint.yaml"] }]
[[repos]]
repo = "https://github.com/DavidAnson/markdownlint-cli2"
rev = "v0.18.1"
hooks = [{ id = "markdownlint-cli2", name = "Markdown Linter" }]
[[repos]]
repo = "https://github.com/google/yamlfmt"
rev = "v0.17.2"
hooks = [{ id = "yamlfmt", name = "YAML Formatter", args = ["-conf", ".github/yamlfmt.yml"] }]
[[repos]]
repo = "https://github.com/asottile/pyupgrade"
rev = "v3.21.2"
hooks = [{ id = "pyupgrade", args = ["--py38-plus"] }]

# djLint is intentionally omitted. It is an HTML/Django-template linter whose
# Jinja rules fight this Copier template's conventions: T001 wants `{{ x }}`
# while the template universally uses `{{x}}` (see CLAUDE.md), and the
# reformatter would rewrite every variable and collapse intentional blank
# lines in the rendered Python files. It never had a `[tool.djlint]` config.
