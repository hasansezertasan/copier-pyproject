[project]
name = "{{github_repo_name}}"
description = "{{short_description}}"
license = "MIT"
readme = "README.md"
authors = [
  { name = "{{author_full_name}}", email = "{{author_email}}" },
]
maintainers = [
  { name = "{{author_full_name}}", email = "{{author_email}}" },
]
dynamic = ["version"]
# TODO @{{github_user}}: Fill in the missing keywords...
keywords = [
    "command-line", "cli", "package", "metadata", "pypi", "python",
    "template", "copier", "scaffold", "boilerplate", "uv", "hatch",
    "tox", "pytest", "ruff", "mypy", "pyright",
    "type-hints", "typed", "modern-python",
    "ci-cd", "github-actions", "automation", "mkdocs",
    "pre-commit", "linting", "testing", "coverage", "release-automation",
    {% if include_cli %}"typer",{% endif %}
    {% if include_web %}"fastapi", "docker",{% endif %}
    {% if include_tui %}"textual", "tui",{% endif %}
    {% if include_pydantic_settings %}"pydantic",{% endif %}
    {% if include_c_extensions %}"cython", "c-extensions", "performance",{% endif %}
]
# TODO @{{github_user}}: Fill in the missing classifiers...
classifiers = [
  "Development Status :: 1 - Planning",
  "Environment :: Console",
  "Intended Audience :: Developers",
  "Intended Audience :: Information Technology",
  "Intended Audience :: System Administrators",
  "License :: OSI Approved :: MIT License",
  "Natural Language :: English",
  "Operating System :: OS Independent",
  "Programming Language :: Python :: 3 :: Only",
  "Programming Language :: Python :: 3",
  "Programming Language :: Python :: 3.10",
  "Programming Language :: Python :: 3.11",
  "Programming Language :: Python :: 3.12",
  "Programming Language :: Python :: 3.13",
  "Programming Language :: Python :: 3.14",
  "Programming Language :: Python :: Implementation :: CPython",
  "Programming Language :: Python :: Implementation :: PyPy",
  "Programming Language :: Python",
  "Topic :: Software Development :: Libraries :: Python Modules",
  "Topic :: Software Development :: Libraries",
  "Topic :: Software Development",
  "Typing :: Typed",
{% if include_cli %}
  "Environment :: Console",
  "Topic :: Utilities",
{% endif %}
{% if include_web %}
  "Framework :: FastAPI",
  "Topic :: Internet :: WWW/HTTP :: HTTP Servers",
{% endif %}
{% if include_tui %}
  "Environment :: Console",
  "Topic :: Terminals",
{% endif %}
{% if include_c_extensions %}
  "Topic :: Software Development :: Compilers",
{% endif %}
]
requires-python = ">=3.10"
dependencies = []

[project.optional-dependencies]
{% if include_cli %}cli = ["typer>=0.20.0"]{% endif %}
{% if include_web %}web = ["fastapi[all]>=0.123.5"]{% endif %}
{% if include_tui %}tui = ["textual>=0.50.0"]{% endif %}
{% if include_pydantic_settings %}config = ["pydantic-settings>=2.0.0"]{% endif %}
all = [
    {% if include_cli %}"cli",{% endif%}
    {% if include_web %}"web",{% endif%}
    {% if include_tui %}"tui",{% endif%}
    {% if include_pydantic_settings %}"config",{% endif%}
]


[project.urls]
{% if include_changelog %}changelog = "https://github.com/{{github_user}}/{{github_repo_name}}/blob/main/CHANGELOG.md"{% endif %}
documentation = "https://{{github_user}}.github.io/{{github_repo_name}}"
homepage = "https://github.com/{{github_user}}/{{github_repo_name}}"
issues = "https://github.com/{{github_user}}/{{github_repo_name}}/issues"
releasenotes = "https://github.com/{{github_user}}/{{github_repo_name}}/releases"
source = "https://github.com/{{github_user}}/{{github_repo_name}}.git"

{% if include_cli or include_tui or include_web %}
[project.scripts]
{% if include_cli %}{{github_repo_name}} = "{{github_repo_name}}.cli:app"{% endif %}
{% if include_tui %}{{github_repo_name}}-tui = "{{github_repo_name}}.tui:main"{% endif %}
{% if include_web %}{{github_repo_name}}-web = "{{github_repo_name}}.web:main"{% endif %}
{% endif %}
{% if include_gui %}
[project.gui-scripts]
{{github_repo_name}}-gui = "{{github_repo_name}}.gui:main"
{% endif %}


[dependency-groups]
dev = [
  "{{github_repo_name}}[all]",
  "tox==4.31.0",
  "tox-uv==1.29.0",
  { include-group = "style" },
  { include-group = "test" },
]
style = [
  "actionlint-py==1.7.9.24",
  "editorconfig-checker==3.6.0",
  "mypy==1.19.0",
  "pyrefly==0.44.0",
  "pyright==1.1.407",
  "ruff==0.14.8",
  "slotscheck==0.19.1",
  "taplo==0.9.3",
  "ty==0.0.1a33",
  "typos==1.40.0",
  "validate-pyproject[all]==0.24.1",
  "vulture==2.14",
]
test = [
  "coverage-enable-subprocess==1.0",
  "coverage[toml]==7.11.0",
  "pytest-dependency==0.6.0",
  "pytest-mock==3.15.1",
  "pytest-rerunfailures==16.1",
  "pytest-xdist[psutil]==3.8.0",
  "pytest==8.4.2",
  "pytest-sugar",
  "dirty-equals",
  "inline-snapshot"
]
docs = [
    "mkdocs==1.6.1",
    "mkdocs-material==9.1.1",
]
tool = [
  {% if include_commitizen %}"commitizen==4.10.0",{% endif %}
  {% if include_poe %}"poethepoet==0.38.0",{% endif %}
  {% if include_c_extensions %}"cython>=3.0.0",{% endif %}
  "copier==9.11.0",
]
pre-commit = [
    "pre-commit-uv==4.2.0",
    "pre-commit==4.5.0",
]


[build-system]
{% if include_c_extensions %}
requires = ["hatchling", "hatch-vcs", "cython>=3.0.0"]
{% else %}
requires = ["hatchling", "hatch-vcs"]
{% endif %}
build-backend = "hatchling.build"


[tool.uv]
add-bounds = "exact"
cache-keys = [
  { file = "pyproject.toml" },
  { git = { commit = true, tags = true } },
]
package = true
trusted-publishing = "always"


[tool.hatch.version]
source = "vcs"
fallback-version = "0.1.0"


[tool.hatch.build]
hooks.vcs.version-file = "src/{{github_repo_name}}/_version.py"


[tool.hatch.version.raw-options]
version_scheme = "only-version"
local_scheme = "no-local-version"

[tool.hatch.metadata]
allow-direct-references = true


[tool.hatch.build.targets.sdist]
include = ["/README.md", "LICENSE", "/{{github_repo_name}}", "/tests"]

{% if include_commitizen %}
[tool.commitizen]
major_version_zero = true
name = "cz_conventional_commits"
tag_format = "$version"
{% if include_changelog %}
update_changelog_on_bump = true
{% else %}
update_changelog_on_bump = false
{% endif %}
version_provider = "scm"
version_scheme = "pep404"
{% endif %}

[tool.pytest.ini_options]
# addopts = "-rXs --strict-config --strict-markers"
addopts = "-v --tb=short"
filterwarnings = ["error"]
testpaths = ["tests"]
xfail_strict = true


[tool.coverage.run]
branch = true
omit = ["src/**/_version.py"]
parallel = true
source_pkgs = ["{{github_repo_name}}", "tests"]


[tool.coverage.paths]
tests = ["tests", "*/{{github_repo_name}}/tests"]
{{github_repo_name}} = ["src/{{github_repo_name}}", "*/{{github_repo_name}}/src/{{github_repo_name}}"]


[tool.coverage.report]
exclude_lines = ["no cov", "if __name__ == .__main__.:", "if TYPE_CHECKING:"]
fail_under = 99
omit = ["src/**/_version.py"]
show_missing = true
skip_covered = true


[tool.vulture]
ignore_decorators = ["@app.command", "@app.get"]
ignore_names = ["CONFIG_FILE_PATH"]
min_confidence = 60
paths = ["src", "tests"]


[tool.slotscheck]
strict-imports = false


[tool.ruff]
fix = true
output-format = "full"
preview = true
show-fixes = true
src = ["src", "tests", "examples"]
target-version = "py310"
unsafe-fixes = true


[tool.ruff.analyze]
detect-string-imports = true
preview = true


[tool.ruff.format]
docstring-code-format = true
line-ending = "lf"
preview = true
skip-magic-trailing-comma = true


[tool.ruff.lint]
fixable = ["ALL"]
ignore = ["COM812", "CPY", "FIX", "TD"]
logger-objects = ["logging_setup.logger"]
preview = true
select = ["ALL"]

[tool.ruff.lint.isort]
combine-as-imports = true
# skip-magic-trailing-comma = true
split-on-trailing-comma = false

[tool.ruff.lint.flake8-copyright]
author = "{{author_full_name}} <{{author_email}}>"


[tool.ruff.lint.flake8-tidy-imports]
ban-relative-imports = "all"


[tool.ruff.lint.mccabe]
max-complexity = 5


[tool.ruff.lint.pycodestyle]
ignore-overlong-task-comments = true


[tool.ruff.lint.pydocstyle]
convention = "pep257"


[tool.ruff.lint.per-file-ignores]
"./tests/*.py" = ["S101", "PLR2004"]
"./examples/*.py" = ["INP001"]


[tool.ruff.lint.pyupgrade]
keep-runtime-typing = true


[tool.ruff.lint.ruff]
parenthesize-tuple-in-subscript = true


[tool.mypy]
files = ["src"]
pretty = true
python_version = "3.10"
strict = true


[tool.pyright]
exclude = [".venv"]
include = ["src/{{github_repo_name}}"]
pythonVersion = "3.10"
venv = ".venv"
venvPath = ".venv"


[tool.ty.src]
include = ["src", "tests"]
respect-ignore-files = false


[tool.ty.environment]
python-version = "3.10"


[tool.ty.terminal]
error-on-warning = true


[tool.pyrefly]
python-version = "3.10"
project-includes = ["src"]


{% if include_poe %}
[tool.poe]
executor.type = "uv"


[tool.poe.tasks.test]
help = "Run tests"
cmd = "tox run"


[tool.poe.tasks.style]
help = "Run Style"
cmd = "tox run -e style"


[tool.poe.tasks.pypi-readme]
help = "Generate Fancy PyPI README"
shell = "hatch-fancy-pypi-readme -o PyPI.md && open PyPI.md"
{% endif %}


[tool.tox]
env_list = [
    "style",
    {% if include_cli %}"cli",{% endif %}
    {% if include_web %}"web",{% endif %}
    {% if include_tui %}"tui",{% endif %}
    {% if include_pydantic_settings %}"config",{% endif %}
    "3.14",
    "3.13",
    "3.12",
    "3.11",
    "3.10"
]
requires = ["tox>=4"]


[tool.tox.env_run_base]
commands = [
  [
    "pytest",
    "-v",
    "--tb=short",
    "--basetemp={env_tmp_dir}",
    { replace = "posargs", default = [
    ], extend = true },
  ],
]
constrain_package_deps = true
dependency_groups = ["test"]
description = "Run the tests under {base_python}"
env_tmp_dir = "{toxworkdir}/tmp/{envname}"
extras = ["all"]
package = "wheel"
runner = "uv-venv-runner"
use_frozen_constraints = true
wheel_build_env = ".pkg"


[tool.tox.env.style]
base_python = ["3.10"]
commands = [
  [
    "ruff",
    "check",
  ],
  [
    "ruff",
    "format",
  ],
  [
    "mypy",
    "--python-version",
    "3.10",
  ],
  [
    "mypy",
    "--python-version",
    "3.14",
  ],
  [
    "pyright",
  ],
  [
    "ty",
    "check",
  ],
  [
    "pyrefly",
    "check",
  ],
  [
    "vulture",
  ],
  [
    "python",
    "-m",
    "slotscheck",
    "src",
  ],
  [
    "taplo",
    "lint",
  ],
  [
    "taplo",
    "format",
  ],
  [
    "validate-pyproject",
    "pyproject.toml",
  ],
  [
    "typos",
    "--diff",
  ],
  [
    "actionlint",
  ],
]
dependency_groups = ["style"]
description = "Run Checkers, Linters and Formatters"
runner = "uv-venv-runner"


[tool.tox.env.pre-commit]
commands = [["pre-commit", "run", "--all-files"]]
dependency_groups = ["pre-commit"]
description = "Run pre-commit hooks"
runner = "uv-venv-runner"


[tool.tox.env.docs-build]
commands = [["mkdocs", "build", "--clean"]]
dependency_groups = ["docs"]
description = "Build Docs"
runner = "uv-venv-runner"


[tool.tox.env.docs-server]
commands = [["mkdocs", "serve"]]
dependency_groups = ["docs"]
description = "Start the live-reloading docs server"
runner = "uv-venv-runner"

{% if include_cli %}
[tool.tox.env.cli]
commands = [["{{github_repo_name}}", "version", { replace = "posargs", extend = true }]]
description = "Run the CLI application."
runner = "uv-venv-runner"
{% endif %}
