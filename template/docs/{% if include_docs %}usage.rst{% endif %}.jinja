Usage
=====

As a library
------------

Look up the installed distribution version:

.. literalinclude:: examples/version_lookup.py
   :language: python
   :caption: examples/version_lookup.py

For short interactive snippets embedded in prose, the ``docs-doctest`` task
executes ``>>>`` blocks too:

.. doctest::

   >>> from {{github_repo_name}}.__metadata__ import PROJECT_NAME
   >>> PROJECT_NAME
   '{{github_repo_name}}'
{%- if include_cli %}

As a command-line tool
----------------------

To use ``{{github_repo_name}}`` as a command-line tool:

.. code-block:: sh

   {{github_repo_name}} version
   {{github_repo_name}} info

Or invoke it programmatically from Python:

.. literalinclude:: examples/cli_usage.py
   :language: python
   :caption: examples/cli_usage.py
{%- endif %}
{%- if include_web %}

As a web application
--------------------

To run the {{web_framework}} web application:

.. code-block:: sh

   {{github_repo_name}}{% if primary_component != 'web' %} web{% endif %}
{%- endif %}
{%- if include_gui %}

As a GUI application
--------------------

To launch the GUI application:

.. code-block:: sh

   {{github_repo_name}}{% if primary_component != 'gui' %} gui{% endif %}

Retrieve the info message without opening a window:

.. literalinclude:: examples/gui_usage.py
   :language: python
   :caption: examples/gui_usage.py
{%- endif %}
{%- if include_tui %}

As a TUI
--------

To launch the terminal user interface:

.. code-block:: sh

   {{github_repo_name}}{% if primary_component != 'tui' %} interactive{% endif %}

Retrieve the info message without starting the TUI:

.. literalinclude:: examples/tui_usage.py
   :language: python
   :caption: examples/tui_usage.py
{%- endif %}
{%- if include_mcp %}

As an MCP server
----------------

To run the Model Context Protocol server:

.. code-block:: sh

   {{github_repo_name}}{% if primary_component != 'mcp' %} mcp{% endif %}

List and invoke tools programmatically:

.. literalinclude:: examples/mcp_usage.py
   :language: python
   :caption: examples/mcp_usage.py
{%- endif %}
{%- if include_worker %}

As a message queue worker
-------------------------

To run the {{worker_broker}} worker:

.. code-block:: sh

   {{github_repo_name}}{% if primary_component != 'worker' %} worker{% endif %}
{%- endif %}
