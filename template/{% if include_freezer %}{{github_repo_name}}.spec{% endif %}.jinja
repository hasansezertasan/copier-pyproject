# PyInstaller spec for {{github_repo_name}} — see ADR-007.
# Build with: pyinstaller {{github_repo_name}}.spec   (or: mise run freeze)
# Produces a single self-contained executable that needs no Python on the target.
# -*- mode: python ; coding: utf-8 -*-

{% if include_cli -%}
entry_point = "src/{{github_repo_name}}/cli/app.py"
{% elif include_gui -%}
entry_point = "src/{{github_repo_name}}/gui/app.py"
{% elif include_tui -%}
entry_point = "src/{{github_repo_name}}/tui/app.py"
{% elif include_web -%}
entry_point = "src/{{github_repo_name}}/web/app.py"
{% else -%}
entry_point = "src/{{github_repo_name}}/__main__.py"
{% endif %}
a = Analysis(
    [entry_point],
    pathex=["src"],
    binaries=[],
    datas=[],
    # TODO @{{github_user}}: add hidden imports if PyInstaller misses any.
    hiddenimports=[],
    hookspath=[],
    hooksconfig={},
    runtime_hooks=[],
    excludes=[],
    noarchive=False,
)
pyz = PYZ(a.pure)

exe = EXE(
    pyz,
    a.scripts,
    a.binaries,
    a.datas,
    [],
    name="{{github_repo_name}}",
    debug=False,
    bootloader_ignore_signals=False,
    strip=False,
    upx=True,
    upx_exclude=[],
    runtime_tmpdir=None,
    console={{ "False" if include_gui else "True" }},
    disable_windowed_traceback=False,
    argv_emulation=False,
    target_arch=None,
    codesign_identity=None,
    entitlements_file=None,
)
