---
services:
  queue:
    build:
      context: ./queue
      dockerfile: Dockerfile
    restart: unless-stopped
    env_file:
      - ./.env
    ports:
      - 5672:5672
      - 15672:15672
networks:
  default:
